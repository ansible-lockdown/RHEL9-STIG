---

- name: POST | AUDITD | Apply auditd template will only required rules will be added
  notify: auditd_checks
  ansible.builtin.template:
    src: etc/audit/rules.d/audit.rules.j2
    dest: /etc/audit/rules.d/audit.rules
    owner: root
    group: root
    mode: '0640'
