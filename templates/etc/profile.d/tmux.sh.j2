# Tmux profile script created by DISA STIG for RHEL9
## Supplied by ansible-lockdown
## Sponsored by Mindpoint Group - A Tyto Athene Company
#!/bin/bash

{% if rhel_09_412015 %}
if [ "$PS1" ]; then
parent=$(ps -o ppid= -p $$)
name=$(ps -o comm= -p $parent)
case "$name" in (sshd|login) tmux ;; esac
fi
{% endif %}
