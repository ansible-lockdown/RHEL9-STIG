cat /etc/grub.d/01_users
#!/usr/bin/sh -e
cat << EOF
if [ -f \${prefix}/user.cfg ]; then
  source \${prefix}/user.cfg
  if [ -n "\${GRUB2_PASSWORD}" ]; then
    set superusers="{{ rhel9stig_grub_superuser }}"
    export superusers
    password_pbkdf2 {{ rhel9stig_grub_superuser }} \${GRUB2_PASSWORD}
  fi
fi
EOF
